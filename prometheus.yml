global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'nats'
    static_configs:
      - targets: ['nats-exporter:7777']

  - job_name: 'zincsearch'
    static_configs:
      - targets: ['zincsearch:4080'] # Scrapes ZincSearch directly